# Normal assembler return
10 return
# Extension allowing to specify arguments
# Expected usage is to allow to mark function live-out registers, but
# parser should support arbitrary expressions (like constants).
20 return $b, 10

// Estimated params: [$eax, $ebx]
void fun_05()
{
  // $sp = $sp_0; // {'uses': []} (dead);
  $ecx = 0; // {'uses': ['40']}
  // $Z = $eax == $ebx; // {'uses': ['20']} (dead);
  // $C = $eax < $ebx; // {'uses': []} (dead);
  if ($eax != $ebx) {
    $ecx = 1; // {'uses': ['40']}
  }
  uses($ecx);
}
